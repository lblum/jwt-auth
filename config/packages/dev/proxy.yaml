parameters:
   app.proxy_list:
      'api-test'  :
         # Dirección remota
         'forward-url' : '%env(LOCALHOST)%/test'
         # Headers adicionales (fundamentalmente para autorización básica)
         'headers-extra' : 
            # El usuario y password van en formato base64
            # Usuario: pass -> base 64
            # https://www.base64encode.org
            'Authorization'   : 
               - 
                  'text': 'Basic '
                  'base64': false
               - 
                  'text': 'rwspu: restws'
                  'base64': true
